micronaut :
  application :
    name : smartmirror-backend
  router :
    static-resources :
      default :
        paths : classpath:frontend
        mapping : /**
        enabled : true
  security :
    reject-not-found : false
    authentication : cookie
    endpoints :
      login :
        path : api/account/login
      logout :
        path : api/account/logout
        get-allowed : true
    redirect :
      enabled : false
    token :
      jwt :
        signatures :
          secret :
            generator :
              secret : ${JWT_GENERATOR_SIGNATURE_SECRET:pleaseChangeThisSecretForANewOne}
    oauth2 :
      clients :
        spotify :
          client-id : ${SPOTIFY_OAUTH_CLIENT_ID:""}
          client-secret : ${SPOTIFY_CLIENT_SECRET:""}
          scopes :
            - user-read-currently-playing
            - user-read-playback-state
          authorization :
            url : https://accounts.spotify.com/authorize
          token :
            url : https://accounts.spotify.com/api/token
            auth-method : client-secret-post
        mso :
          client-id : ${MSO_OAUTH_CLIENT_ID:""}
          client-secret : ${MSO_OAUTH_CLIENT_SECRET:""}
          authorization :
            url : https://marienschule.com/oauth/authorize
          token :
            url : https://marienschule.com/oauth/token
            auth-method : client_secret_post
          openid :
            issuer : https://marienschule.com
r2dbc :
  datasources :
    default :
      url : ${JDBC_URL:`r2dbc:h2:mem:///smartmirror;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE`}
      username : ${JDBC_USER:root}
      password : ${JDBC_PASSWORD:}
      schema-generate : ${JDBC_SCHEMA_GENERATE:create_drop}
      dialect : ${JDBC_DIALECT:mysql}
smartmirror :
  services :
    openweathermap :
      token : ${OPEN_WEATHER_MAP_TOKEN:""}